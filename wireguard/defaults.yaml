wireguard:
  networks: {}

  defaults:
    installed: true

    network:
      active: true
      hostsfile: false

      nodes: {}
      peers: {}
      deleted_hosts: []

      node:
        ipv4_address:
        ipv6_address:
        listen_port: 51820
        private_key:
        dns: []
        mtu:
        table:
        post_up:
        post_down:
        fw_mark:
        save_config:
        preshared_key:
        persistent_keepalive:
        comment:
        public_key:
        allowed_ips: []
        endpoint:
        endpoint_addr:
