wireguard:
  networks: {}

  defaults:
    network:
      enabled: false
      nodes: {}
      peers: {}
      node:
        address:
        addresses: []
        ip:
        listen_port: 51820
        private_key:
        dns: []
        mtu:
        table:
        post_up:
        post_down:
        fw_mark:
        save_config:
        preshared_key:
        persistent_keepalive:
        comment:
        public_key:
        allowed_ips: []
        endpoint:
        endpoint_addr:
